local_costmap:
  global_frame: odom # overwritten in launch
  robot_base_frame: base_footprint # overwritten in launch
  update_frequency: 5.0 #before 1.0
  publish_frequency: 5.0 #before 2.0
  rolling_window: true
  # width: 5 # 10.0 
  # height: 5 # 10.0
  resolution: 0.05 #increase to for higher res 0.025
  transform_tolerance: 1.0
  cost_scaling_factor: 2.0
  inflation_radius: 0.4
    # obstacle_layer /static_layer /inflation_layer 
    #plugins:
        #- {name: static_layer,            type: "costmap_2d::StaticLayer"}
        #- {name: voxel_layer,             type: "costmap_2d::VoxelLayer"}
        #- {name: inflation_layer,         type: "costmap_2d::InflationLayer"}
